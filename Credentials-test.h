//This is a test file with credentials that you can add
//Remove the '-test' from the file name and fill in your info

#ifndef Credentials_h
#define Credentials_h

const char * ssid = "";
const char * password = "";

char dbUser[] = "";
char dbPassword[] = "";
IPAddress server(10, 0, 1, 1);

#endif